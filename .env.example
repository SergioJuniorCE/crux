# ── Electron Desktop App ───────────────────────────────────────────────────────
# No RIOT_API_KEY needed here anymore — the Crux backend handles Riot API calls.
# Configure the backend's API key in backend/.env instead.
#
# After starting the backend (see backend/), set the backend URL in the app's
# Settings panel (default: http://localhost:3001).
